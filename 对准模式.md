# 对准模式

本文档用于记录对准模式拆分后的结构，方便后续维护和 Codex 继续接手。

## 总体目标

对准模式用于在双相机全画幅预览中识别/确认北极星，显示极轴对准覆盖层，并在自动解算、局部跟踪、人工确认之间切换。

拆分后的原则是：

- `DIMM` 负责 Qt UI 连接、弹窗、canvas 设置和组件串联。
- 自动解算、候选选择、局部跟踪、状态机、overlay 文案等逻辑下沉到独立模块。
- `DIMM.cpp` 中保留的对准代码应尽量是“编排”，避免再堆业务细节。

## 主要模块

### `AlignmentCameraCoordinator`

负责进入/退出对准模式时的相机模式切换。

- `preparePreview`：停止当前采集，将已打开相机切到全画幅、连续取图，并设置对准预览帧率。
- `restoreAfterAlignment`：退出对准时恢复连续/外触发模式。

### `AlignmentFlowCoordinator`

负责对准模式启动/停止的流程文案和基础状态判断。

- 启动前 readiness 检查。
- 初始 solve state。
- 等待/停止/启动状态文本。

### `AlignmentFrameCoordinator`

负责每帧入口判断和帧动作决策。

- `shouldAcceptAlignmentFrame`：检查是否处于对准模式、相机索引、帧是否为空、预览节流。
- `nextFrameAction`：根据自动识别开关和当前 solve state 返回：
  - `Disabled`
  - `ManualTrack`
  - `AutomaticTrack`
  - `WaitRetry`
  - `RequestSolve`

### `AlignmentTaskManager`

负责自动解算任务提交、重试、失败退避。

- ManualOnly 下普通自动解算会被阻止。
- 强制重新识别会退出 ManualOnly 并提交完整解算。
- 低置信度会累计次数并拉长重试间隔。

### `AlignmentController`

负责对准解算/跟踪状态更新。

- 自动结果是否应忽略。
- ManualOnly/Tracking 跟踪成功或失败。
- 自动解算成功、预测-only、错误结果的状态更新。
- 跟踪位置同步到 live runtime。

### `AlignmentLocalTracker`

负责局部跟踪。

- 根据确认的北极星位置计算跟踪窗口。
- 在 ROI 内执行质心检测。
- 把局部 ROI 坐标转换回全画幅坐标。

### `AlignmentCandidateController`

负责候选星核心逻辑。

- 复用 solver detections 或进行 GUI 候选检测。
- 根据历史确认点/上次目标点选择候选。
- 记录选中候选到 runtime。
- 无候选时使用兜底质心更新确认点。

### `AlignmentManualSelectionController`

负责人工候选确认相关的纯状态逻辑。

- 候选弹窗节流。
- 候选确认/取消记录。
- 人工确认后进入 `ManualOnly`。
- 人工确认消息和候选列表文案。

### `AlignmentRuntimeCoordinator`

负责进入/退出对准时的 per-camera runtime 重置。

- 清空确认点、候选状态、last frame、solve result。
- 设置初始 solve state 或 Disabled。

### `AlignmentUiPresenter`

负责 UI 文案、日志和 overlay 构建。

- solve state / status 文案。
- 自动解算结果 label/status。
- 跟踪、等待重试、缺帧、已提交等 label。
- `buildAlignmentOverlay`。
- 将确认北极星应用到 overlay，并计算偏离量和 NCP 距离。

## DIMM 中仍保留的职责

`DIMM` 仍保留以下胶水职责：

- `startAlignmentMode` / `stopAlignmentMode`：调用相机协调、runtime 重置、canvas 清理和 UI 状态切换。
- `handleAlignmentFramePacket`：每帧调度，根据 `FrameAction` 调用局部跟踪、自动解算、overlay 刷新。
- `updateAlignmentOverlay`：组织 overlay 输入、候选采集、候选处理、兜底更新和最终 canvas 设置。
- 弹窗相关：`promptAlignmentCandidateSelection` 仍在 `DIMM`，因为它依赖 QWidget。
- UI 实际调用：`setAlignmentSolveLabel`、`setStatusMessage`、`targetCanvas->...`。

后续如果继续拆，可以优先把 `DIMM` 中候选弹窗和 canvas 操作包成更高层的 UI adapter。

## 核心流程

### 启动对准模式

1. `AlignmentFlowCoordinator::validateStartReadiness` 检查是否可启动。
2. 如果当前是 paused live capture，先停止 live。
3. `AlignmentCameraCoordinator::preparePreview` 切换相机到全画幅连续预览。
4. `AlignmentRuntimeCoordinator` 重置每路对准 runtime。
5. 清理 canvas overlay 和候选框。
6. 启动相机采集。
7. UI 切到对准状态。

### 每帧处理

1. `AlignmentFrameCoordinator::shouldAcceptAlignmentFrame` 做入口检查和节流。
2. `prepareAlignmentFramePreview` 更新 canvas 图像并缓存 last frame。
3. `AlignmentFrameCoordinator::nextFrameAction` 决定动作。
4. 根据动作执行：
   - `ManualTrack`：人工确认后局部跟踪，不自动覆盖身份。
   - `AutomaticTrack`：自动确认后的局部跟踪，失败达到阈值后进入重试。
   - `WaitRetry`：显示剩余等待时间。
   - `RequestSolve`：提交完整自动解算。
5. `finishAlignmentFramePreview` 更新 overlay 并记录 last preview 时间。

### 自动解算

1. `requestAutomaticPolarisSolve` 检查模式、相机、controller、last frame。
2. `AlignmentTaskManager::prepareFullSolveRequest` 决定是否提交。
3. `AlignmentTaskManager::submitFullSolve` 提交到 `PolarisSolverController`。
4. `onPolarisSolveFinished` 根据结果更新状态、runtime、UI 和 overlay。

### 人工确认

1. 用户点击“人工确认”后，设置 `m_alignmentSelectionRequested[cameraIndex] = true`。
2. 下一帧 overlay 更新时收集候选星。
3. `promptAlignmentCandidateSelection` 弹出候选编号选择。
4. 选中后：
   - 候选位置写入 confirmed/last target。
   - `AlignmentManualSelectionController::applyManualConfirmation` 进入 `ManualOnly`。
   - 取消该相机正在进行的自动解算。
   - UI 显示人工确认。
5. ManualOnly 下后续帧只做受控局部跟踪，普通自动结果不会覆盖人工结果。

### 退出对准模式

1. `AlignmentCameraCoordinator::restoreAfterAlignment` 恢复相机触发模式。
2. `AlignmentRuntimeCoordinator` 清理 runtime。
3. 清理 canvas overlay。
4. UI 切回 idle / ROI view。

## 关键状态

`AlignmentSolveState`：

- `Disabled`：自动识别关闭或已退出。
- `WaitingFrame`：等待帧。
- `FullSolving`：完整自动解算中。
- `Tracking`：自动识别成功后局部跟踪。
- `RetryWaiting`：等待重试。
- `ManualOnly`：人工确认优先状态。
- `Error`：错误状态。

重要规则：

- `ManualOnly` 优先级高于普通自动结果。
- 普通自动解算不能在 `ManualOnly` 下自动覆盖人工确认。
- 用户点击“重新识别”才允许强制退出 ManualOnly 并重新完整解算。
- 旧任务回调必须通过 generation/taskId 防止污染新任务状态。

## 构建注意

新增或拆分对准模块后，需要确认 `CMakeLists.txt` 中 `ALIGNMENT_SUPPORT_SOURCES` 包含 `.h/.cpp`。

当前主要对准模块包括：

- `AlignmentCameraCoordinator`
- `AlignmentCandidateController`
- `AlignmentController`
- `AlignmentFlowCoordinator`
- `AlignmentFrameCoordinator`
- `AlignmentLocalTracker`
- `AlignmentManualSelectionController`
- `AlignmentRuntimeCoordinator`
- `AlignmentTaskManager`
- `AlignmentTypes`
- `AlignmentUiPresenter`
- `PolarisDetectionPipeline`
- `PolarisTracker`

## 后续维护建议

- 修改 `DIMM.cpp` 对准流程前，优先查看对应 coordinator/controller 是否已有职责归属。
- 新增状态变更优先放入 `AlignmentController` 或 `AlignmentTaskManager`。
- 新增 UI 文案优先放入 `AlignmentUiPresenter`。
- 新增候选星选择逻辑优先放入 `AlignmentCandidateController` 或 `AlignmentManualSelectionController`。
- 新增相机模式切换逻辑优先放入 `AlignmentCameraCoordinator`。
- 避免在 `DIMM.cpp` 中重新展开大段业务逻辑。
