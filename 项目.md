# DIMM 项目速读与实地调试说明

> 给后续 Codex / 维护者看的入口文件。实地调试前先读本文件，再按需读取 `docs/优化计划.md`、相关源码和静态测试。

## 1. 项目定位

本项目是一个 Qt6 + C++17 的 DIMM 双相机观测/测量程序，核心功能包括：

- 双 Galaxy 相机连接、连续采集、硬件触发采集。
- 双路 ROI 跟踪、全画幅重定位、质心测量。
- 北极星识别、星表匹配、对准模式与手动候选星确认。
- 大气参数计算、图表/状态 UI、CSV 结果写入。
- 上位机 TCP 通信与状态/测量结果上报。

主要入口：

- UI 主类：`src/DIMM.h` / `src/DIMM.cpp`
- 程序入口：`src/main.cpp`
- CMake 工程：`CMakeLists.txt`
- UI 文件：`src/DIMM.ui`
- 优化总计划：`docs/优化计划.md`

## 2. 构建环境

当前 CMake 配置依赖：

- Qt：`E:/Softwoare/Qtool/qt/6.11.0/msvc2022_64`
- Galaxy SDK：`E:/Softwoare/Galaxy/GalaxySDK/Development`
- OpenCV：`E:/Softwoare/OpenCV/opencv4120`
- C++ 标准：C++17
- 目标：`DIMM`

用户通常自己在 Visual Studio / CMake Tools 里构建。除非用户明确要求，Codex 不要主动运行：

```text
cmake
msbuild
devenv
ninja
nmake
make
ctest
```

实地调试时如果用户贴构建错误，优先根据错误定位源码/CMake 问题，让用户自行重新构建。

## 3. 绝对不要随便改的部分

以下逻辑是硬件现场最敏感区域。没有明确任务时不要改行为、阈值、顺序、同步模型。

### ROI 更新保护区

不要随意修改：

- ROI 固定尺寸、边界裁剪、位置计算。
- 双相机 ROI 配对提交。
- 质心丢失后的全画幅重定位。
- 全画幅定位后切回 ROI 的条件。
- 相机暂停、移动 ROI、恢复采集的顺序。

重点函数：

```text
DIMM::updateMinuteRoi
DIMM::shouldUpdateRoiForRecentering
DIMM::tryApplyInitialCentroidSettleRoi
DIMM::requestLiveFullFrameRelocalization
DIMM::handleLiveRoiCentroidLoss
DIMM::sanitizeRoi
DIMM::buildCameraCentroidRoi
DIMM::buildLiveCameraRoi
DIMM::readLivePairRoiPosition
DIMM::applyLiveHardwareRois
DIMM::applyLiveFullFrameForRelocalization
DIMM::startDualCameraLocalization
DIMM::commitPairedInitialRoisIfReady
CameraManager::prepareFixedRoi
CameraManager::moveRoi
CameraManager::pausePairForRoiUpdate
CameraManager::resumePairAfterRoiUpdate
```

### 采集流程保护区

不要随意修改：

- 相机打开、启动、停止、关闭顺序。
- 连续触发和外部触发配置顺序。
- 帧 ID / generation 过滤。
- 双相机帧配对和测量样本含义。
- 脉冲发生器启动、停止、切频率时序。

重点函数：

```text
DIMM::onStartCapture
DIMM::onStopCapture
DIMM::stopLiveCapture
DIMM::onCapturedFramePacket
DIMM::handleLiveFramePacket
DIMM::configureLiveCameras
DIMM::advanceLiveAcquisitionGeneration
DIMM::scheduleHardwareTriggerStartupCheck
DIMM::checkHardwareTriggerStartup
DIMM::startHardwarePulseStage
DIMM::startFullFrameLocalizationPulse
DIMM::switchToRoiTrackingPulse
CameraManager::startAcquisition
CameraManager::stopAcquisition
CameraManager::setTriggerMode
CameraManager::configureExternalTrigger
ImageProcessorWorker::processFrame
ImageProcessorWorker::appendDifferentialSample
```

## 4. 当前模块划分

### 主控与状态

- `DIMM` 仍然是主窗口和流程编排中心。
- `CameraTypes.h` 提供 `kCameraCount`、`CameraIndex`、`isValidCameraIndex()`。
- `AppConfig.h` 保存结构化配置快照。

### 配置

- `SettingsDialog.h/.cpp`：设置窗口 UI 和用户输入。
- `ConfigValidator.h/.cpp`：配置校验。
- `ConfigApplicationController.h/.cpp`：把配置应用到主程序和相关回调。
- `ConfigTextUtils.h/.cpp`：配置文本注释/行处理。
- `PathUtils.h/.cpp`：应用目录、相对路径、曝光模板路径处理。

### 图像与测量

- `ImageProcessor.h/.cpp`：ROI 图像处理、质心、差分样本、大气参数。
- `ImageUtils.h/.cpp`：灰度化、Mono8 归一化、阈值单位转换。
- `CentroidQuality` / `CentroidMeasurement` 已用于统一质心有效性判断。

### 对准与北极星

- `AlignmentSession.h/.cpp`：对准会话状态、每相机帧状态、候选星选择、手动确认、fallback centroid 写回。
- `AlignmentTypes.h`：对准状态结构。
- `AlignmentController.h/.cpp`：对准结果保护、手动跟踪状态更新等纯逻辑。
- `AlignmentFrameCoordinator.h/.cpp`：对准帧预览节流和下一步动作判断。
- `AlignmentTaskManager.h/.cpp`：北极星 full solve 提交、重试状态。
- `AlignmentLocalTracker.h/.cpp`：基于已确认北极星位置的局部跟踪。
- `AlignmentUiPresenter.h/.cpp`：对准模式 UI 文案和 overlay 组装。
- `AlignmentCameraCoordinator.h/.cpp`：进入/退出对准模式时的相机预览配置。
- `PolarisDetectionPipeline.h/.cpp`：初始星候选、候选覆盖层、候选选择。
- `PolarisSolver.h/.cpp`：北极星识别、星表匹配、异步 solver controller。
- `PolarisCatalog*`、`AstronomyTransform*`、`StarPatternMatcher*`：星表和匹配计算。

注意：旧的 `AlignmentRuntimeCoordinator`、`AlignmentCandidateController`、`AlignmentManualSelectionController` 已合并进 `AlignmentSession` 并删除。

### 输出与通信

- `ResultWriter.h/.cpp`：CSV 文件打开、排队写入、flush、close。
- `CommManager.h/.cpp`：上位机 TCP 通信协议。
- `PulseGeneratorManager.h/.cpp`：脉冲发生器串口/控制。

## 5. 实地调试优先排查路径

### 构建失败

1. 看错误是编译错误还是链接错误。
2. 链接错误通常检查 `CMakeLists.txt` 是否漏了新增 `.cpp`。
3. 未声明标识符通常检查头文件 include、命名空间、声明/定义是否同步。
4. Qt MOC 相关错误先看类是否需要 `Q_OBJECT`，以及文件是否在 CMake target 里。

常用静态搜索：

```powershell
rg -n "目标符号" src tests CMakeLists.txt
rg --files src tests docs
```

### 相机连接/采集异常

优先读：

```text
src/CameraManager.h
src/CameraManager.cpp
src/DIMM.cpp
```

定位函数：

```text
DIMM::onConnectCameras
DIMM::onStartCapture
DIMM::configureLiveCameras
CameraManager::openAll
CameraManager::startAll
CameraManager::onFrameCaptured
```

先看日志、相机是否 open、trigger mode、frame callback 是否进入。不要先重构采集流程。

### ROI 不跟踪或乱跳

优先读：

```text
docs/current_roi_capture_flow.md
src/DIMM.cpp
src/ImageProcessor.cpp
src/CameraManager.cpp
```

重点确认：

- 两路质心是否有效。
- `initialRoiConfirmed` / `pendingInitialRoiReady` / lost centroid 状态。
- 全画幅重定位是否触发。
- `applyLiveHardwareRois` 是否成功。
- 相机 ROI 能力缓存是否正确。

不要改 ROI 阈值、冷却时间、连续帧数量，除非现场确认就是这些参数问题且用户明确要求。

### 北极星识别/对准失败

优先读：

```text
src/AlignmentSession.cpp
src/PolarisSolver.cpp
src/PolarisDetectionPipeline.cpp
src/StarPatternMatcher.cpp
src/AlignmentUiPresenter.cpp
```

排查顺序：

1. `handleAlignmentFramePacket` 是否接收帧。
2. `requestAutomaticPolarisSolve` 是否提交 solver。
3. `PolarisSolverController::submitFrame` 是否被调用。
4. `detectStarsFromFrame` 检测到多少星。
5. `solveDetectedStars` 匹配数量、RMS、score margin。
6. `onPolarisSolveFinished` 是否被 generation 或 ManualOnly 状态过滤。
7. 手动候选星时看 `AlignmentSession::selectInitialCandidate`、`recordSelectedCandidate`、`applyManualConfirmation`。

### CSV 没写出

优先读：

```text
src/ResultWriter.cpp
src/DIMM.cpp
```

定位函数：

```text
DIMM::initResultFile
DIMM::saveResultRow
DIMM::flushPendingWrites
DIMM::closeResultFile
```

检查存储路径、文件 open 错误、是否有有效测量结果进入 `saveResultRow`。

### 上位机通信异常

优先读：

```text
docs/host_communication_protocol_current.md
src/CommManager.cpp
src/DIMM.cpp
```

定位函数：

```text
DIMM::onCommCommand
DIMM::reportMeasurement
DIMM::reportDeviceStatus
CommManager::sendMeasurement
CommManager::sendDeviceStatus
```

## 6. 静态测试说明

`tests/` 下大多是 Python 静态测试，用来约束结构、调用关系和关键逻辑片段。它们不是硬件集成测试。

常见测试主题：

- `test_image_utils_extraction_static.py`
- `test_config_application_controller_static.py`
- `test_result_writer_static.py`
- `test_alignment_*_static.py`
- `test_polaris_*_static.py`
- `test_live_roi_*_static.py`

如果用户允许测试，可优先只跑相关单个静态测试，而不是全量测试。若用户说“不用测试”，只做 `rg` 静态检查。

## 7. 近期优化状态

已经完成的结构化优化：

- 提取 `ImageUtils`、`ConfigTextUtils`、`PathUtils`。
- 新增 `CameraTypes`，开始统一相机数量和索引校验。
- 拆分 `SettingsDialog`，新增 `AppConfig`、`ConfigValidator`、`ConfigApplicationController`。
- 新增 `ResultWriter`，从 `DIMM` 移出结果文件写入状态。
- 新增并强化 `AlignmentSession`，承接对准会话状态、候选星选择、手动确认和 fallback centroid。
- 删除薄转发的 `AlignmentRuntimeCoordinator`、`AlignmentCandidateController`、`AlignmentManualSelectionController`。
- 北极星 hot pixel 模板增加缓存。
- 质心有效性判断引入 `CentroidQuality` / `CentroidMeasurement`。

仍可后续继续做，但要谨慎：

- 继续减少 `DIMM.h` 暴露的内部状态。
- 进一步统一剩余双相机数组，但 ROI/采集保护区不要机械替换。
- 星表三角索引缓存、质心背景估计性能优化。
- 高斯质心局部窗口限制。
- 全画幅找星内存占用优化。

## 8. Codex 接手建议

每次新任务先做：

```powershell
Get-Content 项目.md
rg -n "用户提到的函数或类" src tests docs
```

改代码前确认：

- 是否触碰 ROI 更新保护区。
- 是否触碰采集流程保护区。
- 是否会改变阈值、默认值、状态含义。
- 是否需要同步改 CMake。
- 是否需要同步改静态测试断言。

完成后报告：

```text
1. 修改摘要
2. 修改文件
3. 是否触碰 ROI：否 / 仅等价移动 / 是
4. 是否触碰采集流程：否 / 仅等价移动 / 是
5. 是否构建/测试
6. 需要现场手动验证的项目
7. 风险
```

## 9. 现场手动验证清单

建议实地按这个顺序验证：

1. 程序启动，设置窗口能打开并保存。
2. 两台相机连接、温度/帧率读数正常。
3. 连续采集能看到两路画面。
4. ROI 初始定位、ROI 跟踪、质心显示正常。
5. 质心丢失后能全画幅重定位并切回 ROI。
6. 硬件触发模式能启动，脉冲频率切换正常。
7. 对准模式能进入/退出，北极星自动识别或手动确认可用。
8. CSV 结果文件能创建并持续写入。
9. 上位机开始/停止上报、状态查询、测量包解析正常。

